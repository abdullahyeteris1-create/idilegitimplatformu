import { NextResponse, type NextRequest } from "next/server"; import { isAdminSessionValid } from "@/lib/auth/adminSession"; import { setActive } from "@/lib/paragraph-exercises/paragraphQuestionAdminRepository";
export async function POST(request: NextRequest, context: { params: Promise<{ id: string }> }) { if (!isAdminSessionValid(request)) return NextResponse.json({ ok: false, error: "Yetkisiz erişim." }, { status: 401 }); try { const body = await request.json(); if (typeof body?.isActive !== "boolean") return NextResponse.json({ ok: false, error: "isActive boolean olmalıdır." }, { status: 400 }); return NextResponse.json({ ok: true, question: await setActive((await context.params).id, body.isActive) }); } catch { return NextResponse.json({ ok: false, error: "Aktiflik değiştirilemedi." }, { status: 500 }); } }
