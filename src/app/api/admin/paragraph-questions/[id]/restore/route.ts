import { NextResponse, type NextRequest } from "next/server"; import { isAdminSessionValid } from "@/lib/auth/adminSession"; import { restoreQuestion } from "@/lib/paragraph-exercises/paragraphQuestionAdminRepository";
export async function POST(request: NextRequest, context: { params: Promise<{ id: string }> }) { if (!isAdminSessionValid(request)) return NextResponse.json({ ok: false, error: "Yetkisiz erişim." }, { status: 401 }); try { return NextResponse.json({ ok: true, question: await restoreQuestion((await context.params).id) }); } catch { return NextResponse.json({ ok: false, error: "Soru geri yüklenemedi." }, { status: 500 }); } }
